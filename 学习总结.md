# AI Agent 入门 · 第一阶段学习总结

## 一、Agent 的本质（一句话）

> **Agent = 大模型（LLM）+ 工具 + 一个 while 循环**

循环过程：`调用模型 → 看它要不要工具 → 执行工具 → 把结果喂回模型 → 重复`，直到模型不再要工具、直接给答案。

---

## 二、四个必须吃透的概念

1. **LLM 是无状态的纯函数**：输入文字 → 输出文字。它没有记忆、不能做事，只会预测下一个 token。
2. **对话是「伪造」出来的**：因为无状态，所以每次调用都要把**完整历史**重发一遍。那个 `messages` 列表就是 Agent 的短期记忆。
3. **四种消息角色**：`system`（规则）、`user`（用户）、`assistant`（模型回复，含工具请求）、`tool`（工具结果）。角色本身没有魔法，只是给文字贴标签。
4. **工具调用的真相**：模型**永远不会**调用函数。它只是输出一段「我想调 XX，参数是 YY」的 JSON；**是你的 Python 代码**去执行，再把结果用 `tool` 消息喂回去。

---

## 三、加工具的「三步曲」（本次练习核心）

| 步骤 | 做什么 | 漏掉的后果 |
|------|--------|-----------|
| 1. 写函数 | 定义真实逻辑，**返回值必须是 `str`** | 报错 / 模型读不到 |
| 2. 写说明书 | 在 `TOOLS` 里描述工具给模型看 | 模型不知道有这工具 |
| 3. 注册 | 在 `AVAILABLE_FUNCTIONS` 加映射 | `KeyError` |

---

## 四、本次踩过的坑（都是宝贵经验）

1. **`base_url` 要带 `/v1`** —— 几乎所有 OpenAI 兼容接口（new-api、one-api 等网关）都以 `/v1` 结尾。漏了会返回 HTML 页面而不是 JSON。
2. **工具函数返回值必须是字符串** —— 模型只能读文字，`return 6`（int）是不行的，要 `return str(6)`。
3. **描述要和实际行为一致** —— `len(text)` 算的是「字」（字符），不是「词」；docstring、`description`、返回值文案要统一。
4. **三步曲容易漏后两步** —— 第一次只写了函数，忘了说明书和注册。这是新手最高频的错误。
5. **Windows 中文乱码** —— 不是代码 bug，运行前 `chcp 65001` 或设 `PYTHONUTF8=1` 即可。

---

## 五、现在能做的事

- 看懂 `minimal_agent.py` 的每一行，并能讲清 ReAct 循环
- 独立给 Agent 添加任意一个新工具（走完三步曲）
- 理解「模型只决策、代码才执行」这一 Agent 开发根本心智模型

---

## 六、工具调用深度辨析

### 1. 模型怎么知道「什么时候」调用工具？

**靠训练 + 工具描述，不是靠写死的规则。** 代码里没有任何 if 判断。

- 训练阶段：模型学到「任务需要它做不到的事（查实时信息、精确计算、操作外部系统）时，应输出工具调用」的模式
- 推理时：结合「工具描述 + 用户问题 + 系统提示」做概率判断

### 2. 模型靠什么「识别」工具？

**靠 JSON 说明书（`name` / `description` / `parameters`），不是函数代码。** 模型看不到 `def calculator(...)` 里的内容。

- `name`：唯一标识，模型输出的 `tool_call.function.name` 必须和 `AVAILABLE_FUNCTIONS` 的 key 完全一致，否则 `KeyError`
- `description`：决定模型能否正确判断「这工具适不适合当前场景」

### 3. 代码里哪几行「触发」工具执行？

真正执行的是 `minimal_agent.py:127` 的 `result = func(**args)`：

```python
for tool_call in message.tool_calls:                     # 124：遍历模型要求的工具调用
    func = AVAILABLE_FUNCTIONS[tool_call.function.name]  # 125：按名字找到真正的函数
    args = json.loads(tool_call.function.arguments)      # 126：JSON 字符串转成字典
    result = func(**args)                                # 127：真正执行函数
```

### 4. 完整数据流

`发消息+tools → 模型返回 tool_calls → 判断有工具调用 → 执行引擎执行函数 → 结果作为 tool 消息喂回 → 再发消息 → 模型给最终答案`

---

## 七、训练 vs 提示词 vs 上下文（能力从哪来）

### 核心判据：是否改变权重

- **训练 = 改权重**（反向传播 + 梯度下降）= 让模型「长本事」，能力永久保留
- **推理/提示词 = 不改权重**（只前向计算）= 引导已有本事「往哪发挥」，换对话就消失

验证方法：新建对话、同样输入、去掉格式提示词，若输出不再按格式，说明模型并未「学会」，只是被提示词引导。

### 三层区分

1. **权重**（训练改变）：模型真正能力，跨对话永久存在
2. **上下文**（messages / RAG / 记忆）：当前调用的输入，让模型「看起来记得」，换对话就没了
3. **提示词**（system prompt 等）：引导发挥方向的文字，也是上下文的一部分

> 只有第 1 层涉及权重改变。第 2、3 层本质都是「喂给模型的文字」。`messages.append(...)` 不是学习，权重没变。

### 工具调用历史演变

- **早期**（无原生工具调用）：靠 prompt engineering 教模型输出 JSON + 手写解析器（ReAct 模式）
- **现代**（有原生工具调用）：训练时专门 fine-tune，API 提供 `tools` 参数和结构化 `tool_calls`，省掉「格式」和「解析」

但**「模型只决策、代码才执行」的本质始终不变**，工具描述写得好不好也始终影响判断准不准。

### 边界情况（进阶）

- **LoRA / Adapter / 软提示**：只训练一小部分参数，也算改权重（属于训练）
- **在线学习**：极少数系统推理时更新权重；主流 LLM API 推理时都不改权重

---

## 八、RAG（检索增强生成）

### 解决什么问题

LLM 的三大问题：**知识截止**、**幻觉**、**不知道私有数据**。RAG 思路很朴素：**先检索，再回答**——回答前先从知识库检索相关内容，塞进 prompt，让模型「照着资料」回答，而不是凭记忆瞎编。

### 五步流程

1. **加载 Load**：读入文档
2. **切分 Chunk**：把长文档切成小块
3. **向量化 Embed**：每块文字 → 一个语义向量
4. **存储 Store**：存进向量库
5. **检索+生成 Retrieve + Generate**：问题转向量 → 找最相似块 → 拼进 prompt → 模型回答

### Embedding 核心概念

- **向量 = 文字的「语义坐标」**：语义相近的文字，向量距离也相近（用**余弦相似度**衡量）
- **两个「嵌入」别混淆**：
  - **token 嵌入**：LLM 输入层的查表，每个 token 一个向量；不对外开放，也不适合检索
  - **sentence 嵌入**：专用 embedding 模型，整段话一个语义向量；RAG 用的是这个

### 词袋模型 vs 真 embedding

| | 词袋模型 | 真 embedding |
|---|---|---|
| 原理 | 数字符重叠 | 神经网络语义向量 |
| 局限 | 只匹配「长得像」，不匹配「意思像」 | — |
| 例子 | 「赚钱」找不到「定价」（sim 0.0） | 「赚钱」正确关联「营收/定价」（sim 0.46）|

> 本地部署 DeepSeek 对话模型 ≠ 有 embedding 模型。Embedding 是单独一类模型（如 BGE），需另外部署或用 sentence-transformers 本地跑。

### 切分策略（RAG 调优核心）

**固定长度 + 重叠**：

- 为什么固定长度：段落长短不一，长段落混合多主题，向量被「稀释」
- 为什么重叠：固定长度会把句子从中间切断，重叠让边界语义至少在一个块里完整

| 参数 | 太小 | 太大 |
|------|------|------|
| `chunk_size` | 语义碎片化 | 稀释 |
| `overlap` | 边界语义丢失 | 冗余、存储翻倍 |

经验值：`chunk_size` 200~500 字（中文），`overlap` 取 10%~20%。

### 升级 embedding 只需改一个函数

把「向量化」抽象成 `embed()` 一个函数，替换底层实现（词袋 → BGE），上层（切分/检索/生成）一行不动。这就是**解耦**。

---

## 九、下一步

- **向量数据库**：现在向量存在内存列表里，学 Chroma / FAISS 做持久化和大规模检索
- **RAG + Agent 结合**：给第一个 Agent 加上「知识库问答」工具（最有价值的实战）
- **LangGraph 工程化**：把手写的 Agent / RAG 用框架重构

---

## 十、RAG 踩坑补充

1. **本地模型加载卡住**：模型已缓存后，`huggingface_hub` 仍会联网校验缓存，网络波动会导致超时。解决：设 `HF_ENDPOINT`（国内镜像）+ `HF_HUB_OFFLINE=1`（离线加载）。
2. **BGE 模型要加查询前缀**：给「问题」加 `为这个句子生成表示以用于检索相关文章：`，文档不加，检索效果更好。

---

## 十一、向量数据库（Chroma）

### 为什么需要向量数据库（相比内存列表）

| 能力 | 内存列表 | 向量数据库 |
|------|---------|-----------|
| 持久化 | 程序退出就丢 | 存磁盘，重启还在 |
| 检索速度 | 线性扫描，O(n) | ANN 索引（近似最近邻），数据量大也快 |
| 动态增删 | 手动管理 list | `add` / `delete` / `update` |
| 元数据 | 无 | 每条向量可附带来源、时间等，支持过滤 |

### Chroma 核心概念

- **`PersistentClient(path)`**：持久化客户端，数据写到本地目录
- **`collection`**：类似数据库的「表」，一个知识库一个集合
- **`metadata={"hnsw:space": "cosine"}`**：用余弦距离衡量相似度
- **距离 vs 相似度**：余弦距离越小越相似，`相似度 = 1 - 距离`

### 核心代码流程

```python
# 建库：向量化 + 写入
collection.add(ids=ids, documents=chunks, embeddings=vectors)

# 检索：问题转向量 -> 查 top_k
results = collection.query(query_embeddings=[q_vec], n_results=top_k)

# 持久化的价值：已有数据就跳过 embedding
if collection.count() == 0:
    build_index(chunks)
else:
    print("直接从磁盘加载，跳过重新 embedding")
```

### 本次测试暴露的关键问题：chunking 决定检索质量

用**固定长度切分**时，短段落「定价分为三档…」只有约 80 字，被硬塞进一个 200 字的块里，和「私有知识库 / 技术支持 / 融资」混在一起，语义被稀释。结果问「这家公司靠什么赚钱？」检索不到「定价」，模型答「资料中没有相关信息」。

**解决：按段落切分（paragraph-aware chunking）** —— 先按空行拆段落，再贪心合并小段落到接近 `chunk_size`；只有单个段落本身超长时，才退化为固定长度 + 重叠硬切。

改完后，同样的「赚钱」问题正确检索到「阶梯式定价策略，已实现规模化营收」，回答正确。

### 踩坑补充

3. **切分策略变了，旧向量就失效了**：向量是「切分结果」的产物，chunk 边界一变，旧向量就不对应了，必须**删掉索引重建**（删 `./chroma_db` 目录或 `collection.delete`），否则会检索到旧数据或 ID 冲突。

---

## 十二、LangGraph（框架 + 工程化）

### 为什么需要框架

手写的 `while` 循环能跑，但项目一复杂就会暴露问题：

1. 多步流程难管理（嵌套 `if` 越写越乱）
2. 状态（`messages`）散落各处，难追踪、难调试
3. 想加重试 / 并行 / 人工确认等能力，要手写一堆逻辑

### 核心思想：把流程画成图

- **节点（Node）**：做什么（调用模型、执行工具）
- **边（Edge）**：下一步去哪（普通边 / 条件边）
- **State**：跨节点共享的短期记忆

### 最重要的心智模型：声明式 vs 命令式

- 手写版是**命令式**：你写 `for`、`if`，一步步指挥 Python 干活
- LangGraph 是**声明式**：你先「声明」图长什么样（节点 + 边），再调用 `.invoke()`，框架的**运行时**去「走」这张图
- 关键：`add_node` / `add_edge` 只是「**描述**」流程，不是「**执行**」；真正的执行发生在 `.invoke()`

### `.invoke()` 背后藏着一个看不见的循环

手写版的 `for _ in range(5)` 现在不在你的代码里，而是藏在 LangGraph 运行时里。运行时做的事：

```
1. 把 {"messages": [System, Human]} 作为初始 state
2. 从 __start__ 沿实线边走到 agent
3. 执行 call_model(state)，返回 {"messages": [AIMessage]}
4. 用 reducer 把返回值合并进 state
5. agent 后是条件边 -> 调 should_continue 得 "tools" 或 "end"
6. "tools" -> 去 tools；"end" -> 去 __end__
7. tools 执行 -> 返回 ToolMessage -> 合并进 state
8. tools 后是实线边 -> 回到 agent（这就是循环！）
9. 重复 3-8，直到某次返回 "end" 走到 __end__ 结束
```

所以：**运行时 = 通用循环 + 你的图定义**。你的 `while` 循环被「参数化」成了图。

### State + reducer（最容易懵、也最关键）

节点为什么返回 `{"messages": [...]}` 而不是完整 state？因为节点只返回**部分更新（增量）**，框架负责合并，合并规则由 **reducer** 决定。

- `Annotated[list, add_messages]` 里的 `add_messages` = **追加（累加）**
- 没有 reducer 的默认行为 = **覆盖**（后写的盖掉先写的），历史会丢

> 实验：把 `Annotated[list, add_messages]` 改成普通 `list`，模型会「失忆」——因为每次只保留最新一条消息。

### 手写版 ↔ LangGraph 对照

| 手写版 | LangGraph |
|--------|-----------|
| `while` 循环 | 图的环 `agent → tools → agent` |
| `messages` 列表 | State（`add_messages` 累加） |
| `messages.append(...)` | `add_messages` reducer 自动累加 |
| `tools=TOOLS` + `json.loads` | `llm.bind_tools(TOOLS)` |
| `if message.tool_calls` | 条件边 `should_continue` |
| `for` 循环执行工具 | 工具节点 `ToolNode` |

### 关键代码

```python
graph = StateGraph(AgentState)
graph.add_node("agent", call_model)     # 节点：调用模型
graph.add_node("tools", tool_node)      # 节点：执行工具
graph.add_edge(START, "agent")          # 边：开始 -> agent
graph.add_conditional_edges("agent", should_continue, {"tools": "tools", "end": END})
graph.add_edge("tools", "agent")        # 边：工具 -> agent（这就是循环）
agent = graph.compile()
```

编译出的实际图（`agent.get_graph().draw_mermaid()`）：

```
__start__ --> agent
agent -. end .-> __end__      # 条件边：路由返回 "end" 时走这条
agent -.-> tools              # 条件边：路由返回 "tools" 时走这条
tools --> agent               # 实线边：循环
```

### 消息流验证（图真的在循环）

执行「计算 (3+5)*2」时产生的消息序列：

`SystemMessage → HumanMessage → AIMessage(tool_calls=['calculator']) → ToolMessage → AIMessage(最终答案)`

和手写 `while` 循环产生的序列**完全一致**，只是循环由图的环自动驱动，不再手写 `for` 和 `if`。

### 关键 API 备忘

- `@tool`：装饰器把函数变成工具，`name` / `description` 从函数名和 docstring 自动推断
- `llm.bind_tools(TOOLS)`：把工具绑到模型，等价手写版传 `tools=TOOLS`（并自动解析 `tool_calls`）
- `add_messages`：State 的累加器（reducer），实现 `messages.append(...)`
- `ToolNode(TOOLS)`：内置工具节点，自动执行 `tool_calls` 并返回 `ToolMessage`
- `should_continue`：条件边路由函数，返回下一个节点名或 `END`

### 工程化收益（手写 while 很难做到）

框架不只是把循环搬过去，还免费送你这些能力：

- **可视化**：`get_graph().draw_mermaid()` 直接画出流程图
- **检查点 / 断点续跑**：每一步的 state 都能存下来，中途失败可从断点继续
- **流式输出**：token 逐个返回
- **重试**：某个节点失败可配置自动重试
- **并行分支**：一个节点后分叉多个节点同时跑
- **人工确认（human-in-the-loop）**：跑到某一步停下等人批准再继续

---

## 十三、RAG + Agent 结合（知识库问答 Agent）

### 核心思想

把「检索」包装成一个工具 `search_knowledge_base`，让 Agent 自己判断「用户问的是不是知识库问题」，是就去检索，不是就不检索。

**图和循环逻辑一行不改，只是 `TOOLS` 列表多了一个工具** —— 这就是框架「加能力 = 加工具」的价值。

### 关键代码

```python
@tool
def search_knowledge_base(query: str) -> str:
    """检索启明星科技的公司资料（产品、定价、融资、团队等）。当用户问相关问题时调用。"""
    retrieved = retrieve(query, top_k=2)
    return "\n\n".join(f"【资料{i+1}】\n{doc}" for i, (_, doc) in enumerate(retrieved))

TOOLS = [search_knowledge_base, calculator, get_current_time, word_count]
```

### 验证：Agent 能区分「知识库问题」和「计算问题」

- 问「启明星科技的主打产品」→ 消息流出现 `AIMessage -> ['search_knowledge_base']`，最终答「星图」Agent 平台
- 问「计算 3*5」→ 消息流出现 `AIMessage -> ['calculator']`，答 15

### 观察：Agent 能「多步检索」

问主打产品时，模型连续调了 **4 次** `search_knowledge_base`（每次换不同子问题补充信息），直到它觉得信息够了才给最终答案。这体现了 Agent 的多轮工具调用能力——图的环允许「检索 → 再检索 → 再检索」直到满意。

---

## 十四、当前进度

- ✅ 第 1 阶段：Python + LLM 概念
- ✅ 第 2 阶段：手写 Agent（工具调用）
- ✅ 第 3 阶段：RAG（词袋 → 真 embedding → 切分 → 向量数据库）
- ✅ 第 4 阶段：LangGraph 基础（状态图、节点、边、条件边）
- ✅ 第 5 阶段：RAG + Agent 结合（知识库问答 Agent）
- ✅ 项目实战 1：企业知识库问答 Agent（见下一节）
- ✅ 项目实战 2：办公自动化 Agent（见「十六」）
- ⏭ 下一站：GitHub 展示 + 求职冲刺

---

## 十五、项目实战：企业知识库问答 Agent（05-kb-agent）

一个可放进简历的完整项目：**RAG 检索 + LangGraph Agent + 引用溯源 + 量化评测 + Gradio Web 界面 + 文档上传**。

### 技术栈

| 模块 | 选型 | 说明 |
|---|---|---|
| LLM | deepseek-v4-pro（OpenAI 兼容） | 本地网关，可通过环境变量替换 |
| Embedding | BAAI/bge-small-zh-v1.5 | 中文向量，带查询指令前缀 |
| 向量库 | Chroma（PersistentClient, cosine） | 持久化到 `chroma_db/` |
| Agent 框架 | LangGraph（StateGraph + ToolNode） | 条件边驱动工具循环 |
| Web 界面 | Gradio 6.x（Blocks + 3 Tabs） | 对话 / 上传 / 评测 |

### 四大亮点与实现要点

1. **Agentic RAG**：检索封装成 `search_knowledge_base` 工具，Agent 自己决定「要不要检索、检索什么」。知识库问题 → 调工具，计算问题 → 调 `calculator`，各走各的路。
2. **引用溯源**：回答标注【资料N】，并从图的 `ToolMessage` 里抽出检索原文展示。
   - 踩坑：Agent 多次检索会把【资料1..N】重新编号，导致回答里的标签和展示对不上。
   - 修复（方案 A）：System Prompt 里约束「尽量只调用一次检索，top_k 一次返回多条」，top_k 从 3 提到 5。
3. **量化评测**（`evaluate.py`）：10 条 QA 对，两个指标——
   - 检索命中率：期望关键词是否都在 top_k 检索结果里（快、确定性）
   - 回答正确率：Agent 回答是否包含所有期望关键词（慢，走完整 Agent）
   - **结果**：检索命中率 90% / 回答正确率 100% / 关键词召回率 90%
4. **文档上传 + 多轮记忆**：上传 `.txt/.md` 自动切块入库（保留来源名）；对话历史存 `gr.State`，支持多轮追问。

### 评测指标的两次修正（很值得记）

- **top_k 对齐**：评测 `retrieve(top_k=3)` 和 Agent 实际 `top_k=5` 不一致 → 改成 5，分数从 50% 涨到 90%。
  > 教训：评测必须「测的是系统真实用到的配置」，否则分数低估真实质量。
- **关键词归一化**：资料写「2 亿元」（带空格）、模型答「7×24」（乘号）导致精确子串匹配误判 → 归一化（去空白、×→x），回答正确率 80%→100%。
  > 教训：关键词级评测很脆弱，要处理格式变体；想更稳得上语义级评测（如 RAGAS）。

### 踩坑记录

| 坑 | 现象 | 解决 |
|---|---|---|
| 引用标签错位 | 回答的【资料N】和展示对不上 | 方案 A：单次检索 + top_k=5 |
| 评测分数异常低 | 检索命中率 50% | top_k 对齐 + 关键词归一化 |
| gradio 旧 API | `gr.Gradio` 不存在 / Chatbot 旧格式报错 | 6.x 用 `gr.Blocks`；Chatbot 必须 `list[dict]`；历史用 `gr.State` 存（Chatbot 回传会被规范化） |
| 离线加载失效 | 启动时连 huggingface.co 超时重试 | app.py 顶部（任何第三方 import 前）设 `HF_HUB_OFFLINE=1` + `TRANSFORMERS_OFFLINE=1`；gradio 先 import 会干扰时序 |
| Windows 中文乱码 | 控制台中文乱码 | `python -X utf8` + 输出重定向到文件再读 |

### 已知局限 & 后续优化

- 200 字切块可能切断词组（「向量检索」被切跨两块）→ 增大 overlap 或按句切分
- 评测是关键词级 → 升级语义级（RAGAS）
- 无 Reranker → 可加 BGE-Reranker 精排
- 会话记忆在内存 → 换 LangGraph Checkpointer 持久化

### 项目亮点话术（面试用）

> 「我用 LangGraph 搭了一个企业知识库问答 Agent，把检索封装成工具让模型自主决定是否调用（Agentic RAG），做了引用溯源（标注来源 + 展示原文），并用 10 条 QA 对做了量化评测——检索命中率 90%、回答正确率 100%。过程中发现关键词级评测容易被格式差异（空格、×/x）误伤，通过归一化和 top_k 对齐把分数从 50% 提到 90%，这说明评测设计和系统配置对齐很重要。」

---

## 十六、项目实战 2：办公自动化 Agent（06-office-agent）

第二个简历项目：**读/写 Excel + 查数据库 + 发邮件**的 LangGraph Agent，CLI 多轮对话。相比项目 1（知识库问答），这套技能树更贴近真实办公业务。

### 技术栈

| 模块 | 选型 | 说明 |
|---|---|---|
| Excel 读写 | pandas + openpyxl | 读表、分组聚合、写报表 |
| 数据库 | SQLite（Python 内置） | 员工表（含邮箱）+ 产品表 |
| 邮件 | smtplib（标准库） | 默认模拟发送到 outbox，可配真实 SMTP |
| Agent 框架 | LangGraph（复用项目 1 模式） | StateGraph + ToolNode + 条件边 |
| 入口 | CLI 多轮对话（cli.py） | 支持带记忆的连续任务 |

### 6 个工具

1. `list_office_files`：列出可用文件，让 Agent 感知资源
2. `read_excel`：读 xlsx 返回文本表格
3. `aggregate_excel`：pandas 分组聚合（sum/mean/count/max/min）
4. `write_excel`：写报表到 output/，返回路径
5. `query_database`：SQLite SELECT（强制只读）
6. `send_email`：SMTP 或模拟发件箱

### 最关键的教训：计算交给工具，不让 LLM 心算

初版让 LLM `read_excel` 拿数据后自己加总 36 行销售额，结果**整张汇总报表全错**：

| 地区 | 真实值 | LLM 心算值 |
|---|---|---|
| 华东 | 692600 | 658600 ✘ |
| 华北 | 560000 | 526400 ✘ |
| 华南 | 739600 | 655400 ✘ |
| 西南 | 559000 | 469000 ✘ |

加一个 `aggregate_excel` 工具，让 pandas 精确聚合，LLM 只负责编排和原样引用数字，结果与 ground truth **完全一致**。

> 面试话术：「我发现让 LLM 读表后心算汇总必然出错，于是把计算下沉到 pandas 工具里，LLM 只负责判断『该用什么工具、按什么分组』，输出精确、可复现。办公自动化的核心是**确定性优先**——凡是能交给代码算的，绝不让模型猜。」

### 安全设计（加分项）

- `query_database` 只允许 `SELECT` 开头，防注入/防破坏
- `write_excel` 只写 `output/` 目录（工具内部拼路径），防越权写任意文件
- 员工表无邮箱字段时，Agent 如实询问用户，而不是瞎编地址（实测它会主动拒绝编造）

### 端到端验证结果

| 场景 | 结果 |
|---|---|
| 华东地区总销售额 | 692600 ✔（工具精确计算） |
| 技术部平均薪资 | 31333.33 ✔（SQL AVG） |
| 各地区销售额汇总成 Excel | 四地区数字全部正确，文件写入 output/ ✔ |
| 给张伟发提醒邮件 | 从库中查到真实邮箱，模拟发送到 outbox，如实说明 ✔ |
| 多轮记忆 | 「我刚才问了什么？」正确回忆起上一轮 ✔ |

### 踩坑记录

| 坑 | 现象 | 解决 |
|---|---|---|
| LLM 心算不可靠 | 汇总报表数字全错 | 加 `aggregate_excel` 工具，计算下沉 pandas |
| 工具对象不可直接调用 | `@tool` 后是 StructuredTool | 用 `.invoke({...})` 传参数 |
| 编造不存在的邮箱 | 上次模拟发邮件时瞎编地址 | 数据里补 email 字段，Agent 查库取真实邮箱 |

### 已知局限 & 后续优化

- 回答措辞可能添油加醋（数字可靠但描述夸张）→ 回答模板化
- 单列分组聚合 → 支持多列/透视表
- 无验收式评测 → 仿项目 1 做「指令 → 期望输出」QA 集（检查生成的 xlsx 数值）
- CLI 无 Web 界面 → 可加 Gradio（项目 1 已展示过该技能）

---

## 十七、两个项目的差异与简历组合

| | 项目 1：知识库问答 Agent | 项目 2：办公自动化 Agent |
|---|---|---|
| 核心 | RAG + Agentic 检索 | 文件/数据库/邮件工具链 |
| 技术亮点 | 引用溯源、量化评测、Reranker 可扩展 | 精确计算、工具安全、防编造 |
| 界面 | Gradio Web（3 Tab） | CLI 多轮对话 |
| 简历卖点 | 「懂检索增强 + 会评测」 | 「懂真实业务工具 + 确定性优先」 |

两个项目互补：一个证明你会**检索增强与评测**，一个证明你会**工具链与工程化安全**，覆盖面试高频考点。

---

## 十八、项目实战 3：多 Agent 调研报告生成器（07-report-agent）

第 3 个简历项目：**手写 Supervisor（监督者）模式的多 Agent 协作**。一个主管 Agent 把「调研报告」任务拆解，派发给「研究员 + 报告撰写员」两个 worker Agent 协作完成，CLI 多轮对话。相比前两个项目（单 Agent + 多工具），这套技能树升级到**多 Agent 编排**——当下 LLM 应用最热门的方向。

### 技术栈

| 模块 | 选型 | 说明 |
|---|---|---|
| 多 Agent 框架 | LangGraph（手写 Supervisor） | 不依赖 `langgraph_supervisor` 包，手写更能讲清原理 |
| Worker Agent | `create_react_agent` | 每个 worker 是独立 ReAct Agent，自带工具 |
| 调研工具 | 知识库关键词检索 + SQLite 查询 | `search_company_kb` / `query_sales_data` / `query_top_products` |
| 报告工具 | `write_report` | 生成 Markdown 报告到 `output/` |
| 入口 | CLI 多轮对话（cli.py） | 支持带记忆的连续任务 |

### 最关键的认知：Supervisor 模式的核心思想（面试重点）

**「Worker 是 Supervisor 的工具」** —— 这是理解多 Agent 协作的钥匙：

- 每个 worker 是**独立的 ReAct Agent**（带自己的工具），分工明确
- Supervisor 也是 LLM，但**不带业务工具**，而是把「每个 worker」声明成自己的**工具**
- Supervisor 通过 `tool_calls` 决定下一步派谁；worker 执行完把结果喂回，Supervisor 再决策 —— 这就是**编排循环**
- 直到 Supervisor 觉得信息够了，调用 `finish_tool` 结束

> 和 LangGraph 官方 supervisor 教程一致：worker 作为 supervisor 的工具，supervisor 用 `bind_tools` 感知 worker 职责，用 `tool_calls` 编排。

### 代码骨架

```python
# 1. 每个 worker 是独立 Agent
researcher_agent = create_react_agent(llm, tools=[search_company_kb, query_sales_data, query_top_products])
writer_agent     = create_react_agent(llm, tools=[write_report])

# 2. 把 worker 包装成 supervisor 的「工具」
@tool
def researcher(query: str) -> str:
    """研究员：负责收集信息..."""
    result = researcher_agent.invoke({"messages": [("human", query)]})
    return result["messages"][-1].content

@tool
def writer(report_request: str) -> str:
    """报告撰写员：把调研结论写成报告..."""
    result = writer_agent.invoke({"messages": [("human", report_request)]})
    return result["messages"][-1].content

@tool
def finish(summary: str) -> str:
    """所有工作已完成，调用此工具结束任务。"""
    return f"任务完成：{summary}"

SUPERVISOR_TOOLS = [researcher, writer, finish]
supervisor_llm = llm.bind_tools(SUPERVISOR_TOOLS)

# 3. 图：supervisor 路由到 worker，worker 执行完回 supervisor
graph.add_conditional_edges("supervisor", should_continue, {"worker": "worker", "finish": "finish", "end": END})
graph.add_edge("worker", "supervisor")   # 这就是编排循环
```

### 端到端验证结果（真实消息流）

跑「调研启明星科技的市场表现，写一份 2024 年市场调研报告」：

```
Supervisor → 派 researcher（调研 Agent）
  ├─ 研究员检索知识库：公司简介 / 产品 / 定价 / 融资 / 团队 / 市场
  └─ 研究员查销售库：各地区销售额、TOP 产品
Supervisor → 派 writer（报告撰写 Agent）→ 生成正式 Markdown 报告到 output/
Supervisor → finish → 返回报告路径
```

生成的报告含：公司概况、产品与商业模式、市场环境与竞争、市场表现与销售数据、结论与建议五大板块，数据来自真实检索（未编造）。

### 踩坑记录

| 坑 | 现象 | 解决 |
|---|---|---|
| 中文查询切词失效 | 「启明星科技的产品和定价」被当成一个整词，检索不到 | 把常见连接词（的/和/与）当分隔符切词 + 兜底用 2~4 字连续片段匹配标题 |
| key 占位符含中文 | `Authorization: Bearer <默认 key>` 含中文，HTTP 头 ASCII 编码崩溃 | 必须通过环境变量传真实 key（这也是清理硬编码 key 的预期结果） |
| 一次多个 tool_calls | Supervisor 同时返回 researcher+writer，只处理第 1 个导致 OpenAI 校验失败 | worker 节点**遍历所有** tool_calls，逐个执行并返回对应 ToolMessage |

### 与之前项目的区别

| | 项目 1/2：单 Agent | 项目 3：多 Agent 协作 |
|---|---|---|
| 模型 | 1 个 LLM + 多个工具 | 多个 LLM（各自独立 ReAct Agent） |
| 编排 | Agent 自己决定调什么工具 | Supervisor 决定派哪个 Agent |
| 分工 | 工具粒度 | Agent 粒度（每个 Agent 带自己的工具） |
| 简历卖点 | 检索增强 + 工具链 | 多 Agent 协作 + 任务拆解 |

### 已知局限 & 后续优化

- 知识库检索用简单关键词 → 换 embedding 语义检索（复用项目 1 的 BGE + Chroma）
- 无评测 → 做「报告是否包含期望信息点」的验收式评测
- 无 Web 界面 → 可加 Gradio
- `create_react_agent` 在新版 LangGraph 标为 deprecated → 后续迁移到 `langchain.agents`

### 项目亮点话术（面试用）

> 「我用 LangGraph 手写了一个 Supervisor 多 Agent 协作系统：一个主管 Agent 把『调研报告』任务拆解，派发给研究员 Agent（检索知识库 + 查销售库）和报告撰写 Agent（生成 Markdown 报告），通过 tool_calls 编排循环直到 finish。核心思想是『worker 作为 supervisor 的工具』——每个 worker 是独立 ReAct Agent 各带工具，Supervisor 只负责决定派谁、传什么任务。这让我理解了单 Agent（一个 LLM 决定调什么工具）和多 Agent（一个 LLM 决定派哪个 Agent）的本质区别。」
