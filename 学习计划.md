# AI Agent 开发学习计划（求职导向）

## 背景与目标

- **当前水平**：有编程基础，会其他语言，Python 不熟
- **学习目标**：做出实际应用项目，最终求职转行
- **建议周期**：约 4 个月（可全职加速）

> 求职时面试官看的是：**能不能用 Agent 解决真实问题 + 有没有拿得出手的项目**。
> 所以核心策略是「快速补 Python + 大量做项目」，而不是陷入纯理论。

---

## 整体路线（五个阶段）

1. 补 Python + 搞懂 LLM
2. 手写 Agent 核心机制（最重要）
3. RAG（检索增强生成）
4. 框架 + 工程化
5. 项目实战 + 求职冲刺

---

## 详细计划（按周）

### 第 1~2 周：补 Python + 搞懂 LLM

**Python 只学 Agent 开发用得上的部分：**
- 函数、类型注解、`dict`/`list`、`json`、`requests`
- 虚拟环境 venv、异常处理

**搞懂 LLM 核心概念：**
- 会用 API 调大模型（OpenAI / DeepSeek / 通义等）
- 理解 `messages`、`system/assistant/user/tool` 四种角色
- `token`、`temperature`、上下文窗口

**交付物**：一个能多轮对话的命令行聊天脚本

---

### 第 3~4 周：手写 Agent 核心机制（最重要）

- 工具调用、ReAct 循环、短期记忆、规划、反思
- **不碰框架**，纯 Python 手写
- 理解四个必懂概念：
  1. LLM 是无状态纯函数
  2. 对话 = 每次重发完整历史
  3. 四种消息角色
  4. 工具调用真相（模型只决策，代码才执行）

**交付物**：能查资料 + 能计算的 Agent（✅ 已完成）

---

### 第 5~6 周：RAG（检索增强生成）

- 向量数据库、Embedding、文档切分、相似度检索
- **交付物**：能回答自己文档内容的「知识库问答 Agent」

---

### 第 7~9 周：框架 + 工程化

- 深入 **LangGraph**（状态机、节点、边、多步工作流）
- 了解 MCP、LangSmith 可观测性、Agent 评测
- **交付物**：把前面手写的 Agent 用 LangGraph 重构一遍

---

### 第 10~13 周：做 2 个完整项目（简历核心）

- **项目 A**：能操作 Excel/数据库/发邮件的「办公自动化 Agent」
- **项目 B**：多 Agent 协作（如「一个 Agent 调研 + 一个 Agent 写报告」）
- **关键**：写好 README、录演示视频、代码放 GitHub

---

### 第 14~16 周：求职冲刺

- 刷 Agent/LLM 面试题
- 总结项目难点（怎么解决幻觉、怎么评测、怎么控制成本）
- 优化简历项目描述，用「问题 → 方案 → 量化结果」的结构

---

## 推荐学习资源

### 入门视频/文档
- OpenAI 官方 Cookbook（GitHub，免费，工具调用/RAG 案例最权威）
- LangChain / LangGraph 官方文档（有中文）
- 吴恩达《Building Agents》短课（DeepLearning.AI，免费）

### Python 速查
- 廖雪峰 Python 教程（中文，快速过语法够用）

### 进阶/求职
- Anthropic《Building Effective Agents》文章（讲 Agent 设计思想，面试常考）
- GitHub 搜 `awesome-llm-agents` 找项目灵感

### 国内大模型 API（都兼容 OpenAI 接口）
- DeepSeek、通义千问（QWQ）、Kimi、智谱 —— 做练习成本都低

---

## 关键提醒

1. **`base_url` 几乎都要以 `/v1` 结尾**（OpenAI 兼容接口约定）
2. **工具函数返回值必须是字符串**（模型只能读文字）
3. **加工具走「三步曲」**：写函数 → 写说明书 → 注册
4. **描述要和实际行为一致**（别把「字」说成「词」）
5. **Windows 中文乱码**：运行前 `chcp 65001` 或设 `PYTHONUTF8=1`

---

## 当前进度

- ✅ 第 1 阶段：Python 环境 + LLM 概念（初步）
- ✅ 第 2 阶段：手写第一个 Agent（工具调用 + 加工具练习）
- ✅ 第 3 阶段：RAG（词袋 → 真 embedding → 切分 → 向量数据库 Chroma）
- ✅ 第 4 阶段：LangGraph 基础（状态图、节点、边、条件边）
- ✅ 第 5 阶段：RAG + Agent 结合（知识库问答 Agent）
- ✅ **项目实战（第 1 个项目）**：企业知识库问答 Agent（`05-kb-agent/`）
  - RAG + LangGraph Agent + 引用溯源 + 量化评测 + Gradio Web 界面 + 文档上传
  - 评测：检索命中率 90% / 回答正确率 100% / 关键词召回率 90%
- ✅ **项目实战（第 2 个项目）**：办公自动化 Agent（`06-office-agent/`）
  - 读/写 Excel + SQLite 查询 + 发邮件 + LangGraph Agent + CLI 多轮对话
  - 关键：把计算从 LLM 心算迁移到 pandas 工具，杜绝数字幻觉
- ✅ **项目实战（第 3 个项目）**：多 Agent 调研报告生成器（`07-report-agent/`）
  - 手写 Supervisor 监督模式：主管 Agent 派发「研究员 + 报告撰写员」协作
  - 调研 Agent 检索知识库 + 查销售库，报告 Agent 生成 Markdown 报告，finish 结束
  - 端到端验证：Supervisor → researcher → writer → finish 全流程真实执行
- ✅ **项目上 GitHub**：学习全阶段代码已推送至 `Monyeva/AI-Agent-Learn`
- ⏭ 下一站：
  1. 求职冲刺：刷面试题、总结难点（幻觉、评测、成本控制）
  2. 录演示视频 + README 打磨（GitHub 仓库导航页）
  3. （可选）再做第 4 个项目：Reranker 精排 / 语义级评测（RAGAS）
